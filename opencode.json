{
  "$schema": "https://opencode.ai/config.json",
  "references": {
    "api": {
      "path": "../ApiTienda",
      "description": "Backend API de la tienda: modelos, endpoints y lógica del servidor"
    }
  },
  "mcp": {
    "playwright": {
      "type": "local",
      "command": ["npx", "-y", "@playwright/mcp@latest"],
      "enabled": true
    }
  }
}
